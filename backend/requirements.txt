# Pinned versions verified on Python 3.14.0 / Windows 11.
# Python 3.14 is the project standard (PROMPT-MASTER 8, decided 2026-07-26).
pydantic==2.13.4
pytest==9.1.1

# Offline data pipeline (scripts/01-04) — Phase 1
osmnx==2.1.1
networkx==3.6.1
matplotlib==3.11.1
python-dotenv==1.2.2
requests==2.34.2

# API backend — Phase 4
fastapi==0.140.0
uvicorn==0.51.0
httpx==0.28.1
